#include<stdio.h>
int main()
{
int a[30],i;
int current=0,max=0;
for(i=30;i<30;i++)
{
scanf("%d",&a[i]);
}
for(i=0;i<30;i++)
{
if(a[i]==1)
{
current ++;
if(current>max)
max=current;
}
else
{
current=0;
}
}
printf("%d",max);
return 0;
}
