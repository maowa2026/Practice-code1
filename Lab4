#intclude<stdio.h>
int main()
{
int a[10],i,pos=0,neg=0,zero=0;
for(i=0;i<10;i++)
{
scanf("%d",&a[i]);
if(a[i]>0)pos++;
else if(a[i]<0)neg++;
else zero++;
} 
printf("%d,%d,%d",pos,neg,zero);
return 0;
}
